# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
train: ../datasets/train.txt # train images
val: ../datasets/val.txt  # val images

# number of classes
nc: 2

# class names
names: ['fire', 'smoke']

